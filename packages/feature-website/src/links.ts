/**
 * The two places off-site the footer and the nav point to.
 *
 * In one file because they are repeated four times each — the marketing nav, its
 * drawer, the docs sidebar and the docs drawer — and a support link that works in
 * three of those is worse than one that works nowhere.
 */
export const REPOSITORY_URL = "https://github.com/hzblj/zyplot";

export const COFFEE_URL = "https://www.buymeacoffee.com/hzblj";
